{
  "name": "tablet-info-card",
  "version": "0.4.0",
  "description": "Reusable Home Assistant Lovelace status card.",
  "type": "module",
  "private": false,
  "scripts": {
    "build": "vite build",
    "dev": "vite --host 0.0.0.0",
    "watch": "vite build --watch",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm run build"
  },
  "dependencies": {
    "lit": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vite": "^7.0.0"
  }
}
